from setuptools import setup, find_packages

setup(
    name="npk-crop-recommendation",
    version="1.0.0",
    description="NPK Crop Recommendation MLOps Pipeline",
    packages=find_packages(),
    python_requires=">=3.9",
)
